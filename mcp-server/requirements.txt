# This package imports app.dependencies / app.services.coach_tools directly from
# backend/app (see docs/MCP_SERVER.md's reuse strategy) — install backend/requirements.txt
# too, not just this file. coach_tools.py imports google-genai at module scope even though
# this server never calls Gemini itself, so that dependency comes along transitively.
#   pip install -r backend/requirements.txt -r mcp-server/requirements.txt

# --- MCP ---
mcp[cli]>=2.0.0

# --- Core Web Server (Streamable HTTP transport) ---
uvicorn[standard]>=0.27.1
pydantic>=2.6.4
pydantic-settings>=2.2.1

# --- Database ---
supabase>=2.5.0
websockets>=14.0,<16.0  # Resolves Supabase realtime & asyncio conflicts, same pin as backend/requirements.txt

# --- Redis (optional; shares the backend's rate limiter class) ---
redis[hiredis]>=5.0.0

# --- Testing ---
pytest>=8.1.1
pytest-cov>=5.0.0
pytest-asyncio>=0.23.5
