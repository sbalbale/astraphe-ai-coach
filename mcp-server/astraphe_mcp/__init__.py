"""Astraphe MCP server — connects a user's Astraphe data to any MCP-compatible AI client.

Deliberately named `astraphe_mcp`, not `app`, so this package can coexist on PYTHONPATH
with `backend/app` without collision — see docs/MCP_SERVER.md for the reuse strategy.
"""
